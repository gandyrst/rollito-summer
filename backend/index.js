/* 
const mysql = require('mysql');

const conecction = createConnection({
    host: 'localhost',
    database: 'rollitosummer',
    user: 'andy',
    password: 'TynuxMS[rx1Oz94)',
    port: 3306
});

conecction.connect(function(error){
    if(error){
        throw error;
    }else{
        console.log('CONEXION EXITOSA');
    }
});
conexion.end();

// module.exports{

// }
 */